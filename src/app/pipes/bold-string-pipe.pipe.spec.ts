import { BoldStringPipe } from './bold-string-pipe.pipe';

describe('BoldStringPipe', () => {
  it('create an instance', () => {
    const pipe = new BoldStringPipe();
    expect(pipe).toBeTruthy();
  });
});
